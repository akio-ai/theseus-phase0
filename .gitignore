# Secrets
.env
.env.local
*.pem
*.key

# Kill switch state
control/.killed
control/.killed.cleared_*

# Backups (never commit DB dumps)
backups/
*.dump
*.sql.gz

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Research Layer: raw source cache (296MB of third-party HTML/PDF captures).
# Provenance lives in each dossier's ## Sources section as URLs; the local cache
# is reproducible from those. Kept out of history deliberately — see
# docs/state/DECISIONS.md (2026-08-05).
research/producers/_sources/

# Canonical DB — never committed from this branch.
migration/

# Editor
.vscode/
.idea/
*.swp
.DS_Store
style_engine/private/
