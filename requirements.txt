# Theseus Phase 0 — Python 3.11+

# Database
psycopg[binary]>=3.1,<4
psycopg_pool>=3.2,<4
pgvector>=0.3,<1

# HTTP
httpx>=0.27,<1

# Env / config
python-dotenv>=1.0,<2

# LLM (Batch 2)
anthropic>=0.40,<1

# Testing (Batch 1 already uses)
pytest>=8.0,<9
pytest-asyncio>=0.23,<1

# Logging / introspection
structlog>=24.1,<25
